tasks:
  - name: prereq
    before: |
      source ./bin/installtfcli
      source ./bin/tfalias
    #  source ./bin/installawscli
vscode:
  extensions:
    - amazonwebservices.aws-toolkit-vscode
    - hashicorp.terraform